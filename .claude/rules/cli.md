## CLI Commands

When instructed to run a CLI command, present it in this format:

```
Command:
Location: imac | server | macbook | games-server
ssh: "<command>"
Logic: "brief explanation of what the command does"
Rollback: <command> or "no rollback"
```
